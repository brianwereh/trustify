import 'package:flutter/material.dart';

class ContentIntro extends StatelessWidget {
  const ContentIntro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}